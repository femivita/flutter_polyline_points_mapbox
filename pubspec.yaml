name: flutter_polyline_points_mapbox
description: A flutter package to get polyline points by either passing the coordinates or mapbox encoded polyline string
version: 1.0.0
# repository: https://github.com/my_org/my_repo

environment:
  sdk: ^3.1.3

# Add regular dependencies here.
dependencies:
  # path: ^1.8.0
  http: ^0.13.5

dev_dependencies:
  lints: ^3.0.0
  test: ^1.24.0
